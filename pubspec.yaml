name: bigfun_voice
description: BigFun full project (voice + auth + wallet placeholders)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🔥 Firebase (latest stable, Flutter 3.24 compatible)
  firebase_core: ^3.8.0
  firebase_auth: ^5.8.0
  cloud_firestore: ^5.8.0

  # 📱 Permissions
  permission_handler: ^11.3.1

  # 🎤 Voice/Agora
  agora_rtc_engine: ^6.5.0

  # 🎨 Fonts & UI helpers
  google_fonts: ^6.2.1
  provider: ^6.1.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/avatar_real.png
