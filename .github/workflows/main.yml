import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

// Use the same name as in your pubspec.yaml
// Example: if pubspec.yaml has name: bigfun_voice
import 'package:bigfun_voice/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Build the app and trigger a frame
    await tester.pumpWidget(const MyApp()); // Replace MyApp with your main widget if different

    // Verify that the app starts with a MaterialApp widget
    expect(find.byType(MaterialApp), findsOneWidget);
  });
}
