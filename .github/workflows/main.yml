name: bigfan_voice_chat  # Changed to follow Dart naming conventions
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # UI & Navigation
  cupertino_icons: ^1.0.2
  
  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.2
  
  # State Management
  provider: ^6.1.2
  
  # Network & API
  http: ^1.2.2
  
  # Voice Chat
  agora_rtc_engine: ^6.3.2
  permission_handler: ^11.3.1
  
  # Preferences & Storage
  shared_preferences: ^2.2.3
  
  # Utils
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/icons/
